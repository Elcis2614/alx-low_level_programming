#include "main.h"
/**
 *_strstr - finds the first occurence a string in another
 *@haystack: the string 1
 *@needle: the character string being searching
 *Return: the pointer to the first occurence of accept of null
 */
char *_strstr(char *haystack, char *needle)
{
	return (strstr(haystack, needle));
}

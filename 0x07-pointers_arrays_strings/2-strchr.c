#include "main.h"
/**
 *_strchr - finds the first occrence of a character into a string
 *@s: the string
 *@c: the character
 *Return: the pointer to the first occurence of c of null
 */
char *_strchr(char *s, char c)
{
	return (strchr(s, c));
}
